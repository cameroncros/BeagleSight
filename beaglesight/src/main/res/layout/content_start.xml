<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".StartActivity"
    tools:showIn="@layout/activity_start">

    <TextView
        android:id="@+id/description"
        android:text="This app allows you to keep track of your bow's scope settings, and also to see nearby targets, and estimate the distances to them."
        android:layout_height="wrap_content"
        android:layout_width="fill_parent"/>

    <Button
        android:id="@+id/manageBows"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/see_configured_bows"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/description" />

    <Button
        android:id="@+id/manageTargets"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/see_nearby_targets"
        app:layout_constraintTop_toBottomOf="@+id/manageBows" />
</androidx.constraintlayout.widget.ConstraintLayout>